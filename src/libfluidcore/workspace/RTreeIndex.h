#pragma once

#include "FluidCoreAPI.h"

namespace FluidCore {

// TODO(M3): O(log N) R*-tree AABB index (Boost.Geometry candidate), TRD §3.4.
class RTreeIndex {
  public:
    RTreeIndex();
};

} // namespace FluidCore
