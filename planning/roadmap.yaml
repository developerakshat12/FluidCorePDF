# Machine-readable milestone state. Update alongside planning/ROADMAP.md.
# status values: pending | in_progress | demo_gate_pending | complete
project: fluidcore-platform
updated: 2026-09-01

milestones:
  - id: M0
    name: bootstrap
    window_weeks: [1, 4]
    status: complete
    exit_criteria: any contributor builds + runs ctest on Linux in <= 15 min
    items:
      - vendor Xournal++; create standalone libfluidcore CMake target   # complete: submodule registered (third_party/xournalpp), CI recursive checkout live; plugin stripping done on fork branch fluidcore-integration — gitlink pin bump deferred until first task touches the X++ stack
      - GitHub Actions matrix (GCC12/Clang16) + sanitizers              # complete: green incl. app-shell job
      - license boundary ADR-0001                                       # complete
      - CONTRIBUTING.md + issue templates                               # complete
    demo_gate: GTK window hosting empty WorkspaceView driven by libfluidcore  # met; M0 exited

  - id: M1
    name: reader-core
    window_weeks: [5, 14]
    status: complete
    items:
      - Poppler loading + thumbnails + continuous scroll                # complete: continuous DocumentPane + GtkPaned resizable ThumbnailSidebar with Cairo surface caching and ThumbnailLayoutTest (TASK-2.1, TASK-2.6; follow-up logged: off-thread worker pool for 200+ page background thumbnail rasterization)
      - Cairo dirty-rect pipeline + LRU tile cache                      # complete: 256MB byte-budgeted PageTileCache with RAII CairoSurfaceHandle, visible-page pinning, and GTK3 partial invalidation (TASK-2.7; zoom/resize handled via explicit cache clear)
      - .xopp companion persistence (AnnotationStore)                   # complete: XoppDocument + AnnotationStore round-trip, coord mapping, stroke add/remove (TASK-2.4, TASK-2.5)
      - live ink overlay (stylus + mouse)                               # complete: InkOverlay over GtkOverlay, pressure-scaled width, Cairo alpha blending, DocumentPane wiring + Ctrl+S save (TASK-2.5)
      - stroke stabilizer (Catmull-Rom/Bezier, <= 20ms latency)         # complete: Centripetal Catmull-Rom (alpha=0.5) with velocity-adaptive deadzone, wet leading-edge zero-lag feedback, and Cairo group alpha isolation (TASK-2.8; algorithmic pipeline lag <= 8.05ms at 125Hz; full end-to-end photon latency to be gated during host integration)
      - undo/redo for fluid objects                                     # complete: pure C++20 UndoStack with 100-depth FIFO capacity bounds, AddStroke/RemoveStroke/ClearPage/Compound commands, eraser hit-testing, scoped DamageRect invalidation, and Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y shortcuts (TASK-2.9)
      - text selection & copy                                           # complete: pure C++20 TextSelection domain model and TextSelectionTest, Poppler-GLib reading-order glyph extraction with sub-millisecond layout caching, multi-page selection intervals, I-beam cursor tool mode, scoped damage invalidation, and Ctrl+C clipboard copy (TASK-2.10)

  - id: M2
    name: squeeze-engine
    window_weeks: [15, 24]
    status: complete
    perf_gates: { squeeze_fps_min: 30 }
    items:
      - SqueezeEngine piecewise mapper + headless tests                 # complete: pure C++20 SqueezeEngine with precomputed O(log N) breakpoint table, partial-compression mid-region mapping, 3-segment partial overlap resolution, alpha clamping [0.04, 1.0], multi-page index resolution, and 9-suite SqueezeEngineTest (TASK-2.11)
      - touch pinch / Shift+Scroll fold-and-pull / margin fold pins      # complete: AnchorSqueezePlanner unified multi-anchor engine, SqueezeEngine highlight/search/preview layers, DocumentPane continuous gesture handling w/ re-entrancy protection and cursor pinning, Ctrl+Shift+0 / Ctrl+Shift+R global reset
      - slice-clipping render path                                      # complete: SqueezeRenderHelper slice decomposition with pixel-snapped boundary continuity, cross-crease stroke & selection subdivision, Cairo slice-clipped page blits with fold shadow creases, margin fold pins, and SqueezeRenderTest (TASK-2.12)
      - search-driven squeeze via FTS5 hit intervals                    # complete: AnchorSqueezePlanner interval-union algorithm with context padding, layered search vs user fold state in SqueezeEngine, DocumentSearchService async Poppler search worker with inverted Cairo coordinate space alignment, floating SearchBarWidget overlay [Ctrl+F / Ctrl+Shift+S], search highlight rendering, and SearchSqueezePlannerTest (TASK-2.13)
      - highlight-driven squeeze (HighlightView)                        # complete: Ctrl+Shift+H accelerator, AnnotationStore stroke extraction, excerpt source interval synchronization

  - id: M3
    name: workspace-excerpts
    window_weeks: [25, 38]
    status: complete
    items:
      - infinite canvas pan/zoom + R*-tree index                        # complete: 2D affine transform matrix (M_view), smooth focal zoom (5% to 1000%), pan gestures, zoom-adaptive infinite dot-grid, interactive minimap HUD with glowing viewport frame, O(log N) viewport culling, and 100k items benchmark with p99 <= 0.05ms << 1.0ms budget (TASK-3.1)
      - drag-out excerpts (text + image)                                # complete: ExcerptCardNode pure domain model, binary/string ExcerptPayload serialization, InsertNodeCommand / RemoveNodeCommand undo/redo, InkOverlay drag source with SqueezeEngine document space unprojection, WorkspaceView drag destination, and rich Cairo card rendering with elevated shadow, header badge, return pill, and wrapped text (TASK-3.2)
      - unified inking & real-time continuous eraser across panes       # complete: InkOverlay & WorkspaceView pen/highlighter/eraser tool switching w/ P/H/E/S hotkeys, live stabilizer curve rendering, real-time spatial hit erasure, and undo/redo
      - ExcerptCardNode live re-render                                  # complete: PdfDocumentService multi-document handle isolation and thread-safe Poppler lifecycle, ExcerptTileCache 128 MB byte-bounded LRU tile cache with discrete 0.5x-8.0x LoD tiers clamped to 1536px, asynchronous GThreadPool rasterization with generational cancellation, WorkspaceView 150ms zoom-settling debounce and high-DPI bilinear crop rendering, InkOverlay visual crop tool mode with marquee drag-and-drop, and ExcerptTileCacheTest (TASK-3.4)
      - bi-directional anchors + ReturnAnchorPill                       # complete: WorkspaceView [ ↗ Anchor ] hit-testing, SqueezeEngine auto-expansion, docYToScreen zoom contract, Cairo luminous pulse highlight with cancel-and-replace decay timer, floating glassmorphic ReturnAnchorPill overlay, smooth cubic ease-out camera gliding (glideToWorldCoord), active card focus aura, and 19/19 passing CTest suites (TASK-3.3)
      - workspace undo/redo & top toolbar                               # complete: ToolManager central dispatch, TopToolbarWidget floating glassmorphic pill with tool switching/undo/redo/zoom/minimap/search/export, UndoStack macro transactions (beginMacro/endMacro/abortMacro), CreateInkLinkCommand, RemoveEdgeCommand, strict pane-isolated undo routing, Esc key pointer reset

  - id: M4
    name: links-stacks-search
    window_weeks: [39, 48]
    status: complete
    items:
      - ink connectors -> GraphEdge + Bezier routing                    # complete: pure domain GraphEdge & GraphTopology model, bidirectional promotion, straightness classification, Alt+6/F6/A/L connector tool, edge selection & deletion (TASK-4.1)
      - card snapping/stacking (16pt proximity)                         # complete: pure C++20 PhysicsSolver, screen-adaptive 16pt magnetic alignment, >50% overlap stack merge, CardStackNode container, accordion deck & child extraction, bounds sync (TASK-4.2)
      - workspace search & scoped find                                  # complete: pure C++20 WorkspaceSearchEngine full-text & tag search (#tag, tag:xyz), SearchBarWidget scope selector (Doc, Canvas, All), WorkspaceView 250ms camera glide navigation, WorkspaceRenderer golden match halos & minimap hit dots, DocumentPane cross-pane coordination (TASK-4.3)
      - export flattened annotated PDF + Markdown outline               # complete: pure C++20 WorkspaceExportEngine synthesis exporter (stacks, excerpts, visual crop metadata notes, free notes, relational links, and Mermaid flowcharts), FluidCoreAPI export methods, PdfExportService Cairo vector PDF flattening pipeline, and GTK3 ExportDialog with Ctrl+E accelerator (TASK-4.4)

  - id: M5
    name: hardening
    window_weeks: [49, 58]
    status: pending
    perf_gates: { cold_load_50pdf_s_max: 8, ram_gb_max: 1.2 }
    items:
      - .ltproj WAL finalization + kill -9 recovery suite
      - full round-trip persistence acceptance test (.ltproj save -> close -> reopen -> diff)
      - palm rejection tuning + stylus matrix
      - keyboard-only accessibility pass
      - i18n framework (EN, DE, ZH)

  - id: M6
    name: v1.0-release
    window_weeks: [59, 62]
    status: pending
    items:
      - Flatpak/AppImage/deb; Windows beta
      - docs site + persona screencasts
      - offline security audit
      - v1.0 tag + launch

perf_budgets:
  inking_latency_ms_max: 20
  squeeze_fps_min: 30
  spatial_query_p99_ms_max: 1
  cold_load_50pdf_s_max: 8
  ram_working_set_gb_max: 1.2
